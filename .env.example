# Google Service Account — mismo que MANDARINA CRM
GOOGLE_SERVICE_ACCOUNT_EMAIL=mandarina-pro-sheets@gen-lang-client-0034534950.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Google Sheets — WhatsAppINDLovers (Sheet de IND Store)
SHEET_ID=1ObNIff1ypeFW7PfuAjeoiGBJCDyZU4etIsbGpyB-Nqk

# Make — YA NO SE USA. El envío y la recepción ahora van directo a Meta.
# (Puedes borrar MAKE_SEND_WEBHOOK y apagar los escenarios de Make.)
# MAKE_SEND_WEBHOOK=

# Meta / WhatsApp Cloud API — SOLO server-side (nunca en el cliente)
# Lo usan: /api/saliente (enviar), /api/webhook (recibir),
#          /api/media (bajar media entrante) y /api/upload-media (subir video saliente)
META_TOKEN=EAAxxxxxxxx...
META_PHONE_ID=1092674123940116

# Verify token del webhook de Meta — pon cualquier string aleatorio y usa EL MISMO
# en Meta → WhatsApp → Configuration → Callback URL: https://<tu-app>.vercel.app/api/webhook
WHATSAPP_VERIFY_TOKEN=pon-aqui-un-string-aleatorio
